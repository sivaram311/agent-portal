{
  "name": "agent-portal-mcp-bridge",
  "version": "1.1.0",
  "description": "MCP SSE/HTTP bridge so Gemini Spark can drive Agent Portal / Cursor CLI",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "start:dev": "node --env-file=.env server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^4.21.2"
  }
}
