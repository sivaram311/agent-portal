spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/agentportal
spring.datasource.username=${POSTGRES_USER:agent}
spring.datasource.password=${POSTGRES_PASSWORD:agent}
spring.datasource.driverClassName=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=false
