PUBLIC_HOST=103.118.183.185
POSTGRES_PASSWORD=agent

CSS_ENABLED=true
# When using nginx for delena.buzz, set CSS_AUTH_URL to https://delena.buzz (proxied /auth → css-next :5910)
CSS_AUTH_URL=https://delena.buzz
CSS_JWKS_URI=http://127.0.0.1:5910/.well-known/jwks.json
CSS_ISSUER=https://css-next.delena.buzz
# password | hybrid | oauth — hybrid keeps password form + SSO button
CSS_AUTH_MODE=hybrid
# Must NOT be under nginx /auth/ (proxies to IdP). SPA path via try_files / ng serve.
CSS_OAUTH_REDIRECT_PATH=/oauth/callback
# Phase 1 sandbox (junctions under E:\MyWorkspace\sandbox)
AGENT_WORKSPACE_ROOT=E:\MyWorkspace\sandbox
# Optional comma-separated absolute prefixes outside the sandbox
AGENT_WORKSPACE_ALLOWED_ROOTS=E:\MyWorkspace,E:\Source

# Cursor CLI — use absolute agent.cmd path (Java cannot resolve bare "agent" without user PATH)
CURSOR_AGENT_CMD=C:\Users\Administrator\AppData\Local\cursor-agent\agent.cmd
CURSOR_API_KEY=

# Cloudflare Zone Edit token (set in real .env only — never commit secrets)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_ZONE_NAME=delena.buzz
CLOUDFLARE_ACCOUNT_ID=
